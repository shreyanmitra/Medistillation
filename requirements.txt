# Core PyTorch and ML frameworks
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# HuggingFace libraries
transformers>=4.35.0
accelerate>=0.25.0
peft>=0.7.0
datasets>=2.14.0

# Quantization support (for 8-bit training)
bitsandbytes>=0.41.0

# Training utilities
tqdm>=4.65.0
tensorboard>=2.14.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Evaluation libraries (for your benchmarks)
scikit-learn>=1.3.0
scipy>=1.11.0

# Configuration and logging
pyyaml>=6.0
wandb>=0.15.0  # Optional: if you want to use Weights & Biases for logging

# Tokenization support
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional but recommended
einops>=0.7.0  # For efficient tensor operations
safetensors>=0.4.0  # For safer model serialization